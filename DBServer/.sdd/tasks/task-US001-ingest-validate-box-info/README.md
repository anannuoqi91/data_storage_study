# 任务：US001 - 接收并校验 box_info 帧

## 做什么
补强 `box_info` 帧接入、字段校验、异常隔离和分区状态更新。

## 为什么做
这是所有 release 的起点，脏数据一旦穿透到正式数据面，后面的封口、派生数据和查询都会一起污染。

## 做完什么样
有效帧进入缓冲和分区状态；异常帧整帧拒收并记录到隔离区；设备和晚到窗口约束可验证。

## 依赖
- 前置：无
- 共享实体：FrameBatch、BoxRecord、PartitionState

## 验收标准
见 `tests/acceptance.md`。
